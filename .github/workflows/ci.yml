name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: swift version
        run: swift --version
      - name: test
        run: swift test
      - name: release build
        run: swift build -c release
